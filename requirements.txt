# ─── MLflow core ───
mlflow==3.7.0

# ─── Backend store (PostgreSQL / Neon) ───
# psycopg2-binary : pre-compiled PostgreSQL adapter (no C compilation needed)
# sqlalchemy      : ORM used by MLflow to interact with the database
psycopg2-binary==2.9.10
sqlalchemy==2.0.36

# ─── Artifact store (AWS S3) ────
# boto3 : AWS SDK — used by MLflow to push/pull artifacts from S3
# s3fs  : S3 filesystem interface — required by MLflow artifact store
boto3==1.35.79
s3fs==2025.12.0

# ─── Utilities ────
python-dotenv
requests